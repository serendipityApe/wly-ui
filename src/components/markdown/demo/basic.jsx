import React from 'react';
import { Markdown } from 'wly-ui';

export default () => (
  <Markdown>{`# Scrollbar\r
## Custom scrollbar\r
---\r
Custom scrollbars are getting more popular nowadays, and I’m very keen to dig into them. There are different reasons why to customize a scrollbar. `}</Markdown>
);
