import React from 'react';
import { Collapse } from 'wly-ui';
// import '../style'

const { Panel } = Collapse;

export default () => (
  <div>
      <Collapse>
      </Collapse>
    <Panel header="测试">展开</Panel>
  </div>
);
